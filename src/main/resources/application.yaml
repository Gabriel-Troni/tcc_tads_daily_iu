spring:
  application:
    name: daily-iu-services

  # --- MongoDB ---
  data:
    mongodb:
      uri: mongodb://backend-dailyiu-mongo-sandbox:REMOVED_SECRET==@backend-dailyiu-mongo-sandbox.mongo.cosmos.azure.com:10255/?ssl=true&retrywrites=false&replicaSet=globaldb&maxIdleTimeMS=120000&appName=@backend-dailyiu-mongo-sandbox@
      database: daily-iu

  # --- JPA  ---
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
